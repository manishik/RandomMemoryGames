package com.manish.randomgengames.model;

public enum NameMode {
    // Uses first names only.
    FIRST,

    // Uses last names only.
    LAST,

    // Uses a first name and last name together.
    FULL
}
